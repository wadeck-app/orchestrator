$route: /jobs/:id/edit

$sources:
  jobData:
    url: GET /api/jobs/{id}
    params:
      id: $route.id

$type: PageContent
sections:
  - $type: BackLink
    label: Back to job
  - $type: JobFormSection
    jobId: $route.id
    initial: $sources.jobData
