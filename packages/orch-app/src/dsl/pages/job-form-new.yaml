$route: /jobs/new

$type: PageContent
sections:
  - $type: BackLink
    to: /
    label: Back to jobs
  - $type: JobFormSection
    $id: jobForm
    $outputs:
      onSubmit: [formData]
      onSuccess: [id]
