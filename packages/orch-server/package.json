{
  "name": "@wadeck-app/orch-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "test": "vitest run",
    "copy-app": "node scripts/copy-app.mjs",
    "build:full": "npm run build && npm run copy-app"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "@fastify/static": "^8.0.0",
    "@fastify/cors": "^10.0.0",
    "@wadeck-app/singleton-daemon-kit": "*"
  },
  "devDependencies": {
    "typescript": "*",
    "tsx": "*",
    "vitest": "*",
    "@types/node": "*"
  }
}
