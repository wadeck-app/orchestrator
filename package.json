{
  "name": "orchestrator-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "deploy": "node scripts/deploy-dev.mjs"
  },
  "dependencies": {
    "@wadeck-app/dsl-renderer": "^2026.9.12-061-88bc5d17",
    "@wadeck-app/dsl-ui": "^2026.9.12-061-88bc5d17"
  }
}
